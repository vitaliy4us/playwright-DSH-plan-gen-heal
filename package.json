{
  "name": "playwright-dsh-plan-gen-heal",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests for the GreenKart demo shop, produced with the Playwright Test Agents workflow (planner → generator → healer).",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "trace": "playwright show-trace"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0"
  },
  "dependencies": {
    "@playwright/cli": "^0.1.18"
  }
}
